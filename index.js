
$("h1").css("color","red");

// dollar sign is used to write  jquery
// this dollar sign will target the h1 and the css of h1 and then it will change the color to red.


// $(Document).ready(function({$("h1").css("color","red")});

// this means that jquery will only execute 
// $("h1").css("color","red");
// when we have jQuery loaded in our system



// NOTES

// With a querySelector statement, you can select an element based on a CSS selector. This means you can select elements by ID, class, or any other type of selector. Using the getElementById method, you can only select an element by its ID. SO THE BEST ONE IS QUERY SELECTOR(ID/CLASSNAME/TAG)

// using jQuery we can replace
// document.querySelector('h1');

// by simply typing
// $('h1');

// it even replaces document.querySelectorAll("button") as $("button") selects and returns all the buttons